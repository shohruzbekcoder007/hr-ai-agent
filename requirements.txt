# =============================================================================
# HR AI Agent — Python dependencies
# Hermes Agent Framework is installed from GitHub in the Dockerfile.
# Pin upper bounds to limit supply-chain risk (Hermes dependency policy).
# =============================================================================

# Web API
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# HTTP client (health / internal)
httpx>=0.28.0,<1.0.0

# Config & validation
PyYAML>=6.0.2,<7.0.0
python-dotenv>=1.0.1,<2.0.0

# Structured logging helpers
python-json-logger>=2.0.7,<4.0.0

# Typing (stdlib-first; keep for older tooling)
typing-extensions>=4.12.0,<5.0.0

# ---------------------------------------------------------------------------
# Hermes Agent Framework (installed in Docker build from GitHub)
# Uncomment for local non-Docker development if you prefer pip over Dockerfile:
# hermes-agent @ git+https://github.com/NousResearch/hermes-agent.git
# ---------------------------------------------------------------------------
